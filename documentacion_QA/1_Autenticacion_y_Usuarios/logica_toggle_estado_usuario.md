# Logica: Toggle Estado Usuario

Ver archivo `logica_desactivar_usuario.md` — esta funcionalidad esta documentada ahi de forma completa.

El toggle de estado permite activar o desactivar un usuario desde la vista `adminusuarios.php` usando la accion `toggleEstado` del controlador `adminusuariocontroller.php`.
